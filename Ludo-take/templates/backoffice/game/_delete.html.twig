<form method="post" action="{{ path('backoffice_game_delete', {'id': game.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette catégorie?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ game.id) }}">
    <button class="btn">Supprimer</button>
</form>
